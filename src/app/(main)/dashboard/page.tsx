"use client";
import { SignedIn, SignedOut, SignInButton, UserButton } from "@clerk/nextjs";
const Dashboard = () => {
  return (
    <div className="container mt-20 bg-slate-600">
      <div></div>
    </div>
  );
};

export default Dashboard;
